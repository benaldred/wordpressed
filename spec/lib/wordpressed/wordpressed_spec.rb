require "spec_helper"

describe "Wordpressed" do

  describe "generate_archive_links" do
    it "generate archive links" do
      #Wordpressed.generate_archive_links([]).should == "foo"
      pending
    end
  end
end